server:
  port: 8080
spring:
  redis:
    host: localhost
    port: 6379
  cache:
    type: redis
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3307/sample
    username: root
    password: password
    mapper-locations: classpath:/mapper/*.xml
file:
  upload-dir: /upload